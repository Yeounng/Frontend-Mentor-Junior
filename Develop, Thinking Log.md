# Frontend Thinking Log

> 날짜: 2026-**-**  
> 작업 유형: (기능 추가 / UI 개선 / 리팩터링)

---

## 1. 무엇을 만들거나 고치려고 했는가? (상황)

**Problem 1**
Grid 사용으로 레이아웃 짜던 도중 grid item들의 높이가 너무 높아서 레이아웃 길어보였음
fr을 사용해서 비율대로 쪼겠는데 width는 괜찮으니까 height만 줄이자

**Problem 2**
하단 추천아이템 항목에 레이아웃이 약간 틀어져있었다->grid이후 flex가 다시 적용됐어서 생긴 문제 ->

**Problem 3**
모바일메뉴에서 버튼누르면 우측에서 메뉴 들어오는거 제작. position을 none에서 block으로 구성하면 아에 렌더링트리에서 제외하기때문에 transition과 같은 애니메이션이 작동하지 않고, 단순 위치를 밀어서 안보이게만 하면 모바일사이즈에서 다시 커졌을때 보이게 해두진 않아도 우측에 배치한 요소를 인식하기때문에 레이아웃이 틀어진다.

## 2. 사용자 관점에서의 문제 정의

## 3. 내가 세운 가설 (왜 이렇게 하면 좋을까?)

## 4. 고민했던 선택지들

**Problem 1**

1. fr 버리고 repeat(3,200px) 과 같은 고정값 사용
2. minmax사용 -> 최소는 우선 비율대로 1fr로 하면서 max는 200px로 하면 되지않을까? 했는데 그런식으로 작동 X fr이 max값보다 커질 수는 값이여서 X
3. repeat(3, min(200px,1fr)) css min()함수를 사용해 두 값을 비교해 적은 값을 적용시키게 하기
4. repeat(3, 1fr) 기존 코드대로 적용하고 grid-item에 max-height 적용하기 -> grid track은 비율대로 1fr을 계산해서 적용. 예를들어 부모가 900px이면 1fr은 300px가 track height. -> 여기서 track height는 그대로인데 내부 콘텐츠만 200px 높이 한계가 지정됨. -> grid track과 아이템 사이에 불필요한 공백과 빈공간이 생김

**Problem 2**

- grid아이템 자식 항목에 또 grid를 만들지 말란 법은 없다. flex로 배치하니 간격이 조금 비틀린다면 grid를 적용해서 1fr로 나누자

**Problem 3**

- 우선 위치와 상관없이 모바일 메뉴가 존재해야 함으로 position:fixed적용
- fixed로 우측 상단에 배치하고 opacity 0, visibility:hidden 속성을 주어 상호작용이 되지 않게끔 설정
- -> 결과적으로 우측 상단에 항상 있지만 보이지않고 숨겨져있음. absolute 일때 요소를 translateX(100%)로 화면 밖으로 밀어내면, 브라우저는 이 요소를 문서(Document)의 전체 영역 중 일부로 계산. ex-> 뷰포트는 1000인데 1200위치에 두면 가로 스크롤바를 만들어 보여주려 함. so-> 레이아웃 깨짐
- fixed는 뷰포트(브라우저 창) 을 기준으로 배치. 문서 영역의 일부로 인식하는것과 다름. translateX로 밀어내도 문서 가로너비에 포함시키지 않는다. 특정 브라우저나 구버전 환경에서 여전히 화면 밖 요소를 인식하려 할 수 있기 때문에 보완책-> 최상단 부모에 overflow-x: hidden

## 5. 최종 선택과 이유 ⭐️ 문제 해결

1. 선택지고 뭐고간에 그냥 기본적인 생각 자체를 잘못하고있었다.. 3\*2 그리드인데 메인 아이템이랑 아래 추천아이템이랑 크기를 1fr 1fr로 했다는거 자체가 이상했음. 2fr 1fr로 2열로 만들었더니 괴상하게 레이아웃이 스크롤도생기고 길어지고 그런 문제가 해결됐다.
   grid로 landing page나 index 페이지 만드는 연습을 몇번 더 해봐야겠다.

## 6. 실제로 구현하면서 느낀 점

- 생각보다 쉬웠던 점 / 어려웠던 점
- 막혔던 부분은 무엇이었는가?

### 예시

- 상태를 어디서 관리해야 할지 처음에 헷갈렸다.
- 버튼 컴포넌트가 너무 많은 역할을 하고 있다는 걸 깨달았다.

---

## 7. 결과는 어땠는가? (정성적)

※ 숫자 없어도 괜찮다

- 내가 보기에 UX는 좋아졌는가?
- 이전보다 명확해졌는가?

### 예시

- 클릭 후 바로 반응이 보여서 훨씬 자연스럽다.
- 내가 사용해도 덜 답답하게 느껴진다.

---

## 8. 다시 한다면 바꾸고 싶은 점 (회고)

- 지금 시점에서 아쉬운 선택은?
- 다음에 비슷한 상황이 오면 어떻게 할까?

### 예시

- 상태 관리 로직을 컴포넌트 밖으로 빼고 싶다.
- 공통 버튼 컴포넌트로 정리해보고 싶다.

---

## 9. 이번 작업을 통해 배운점, 활용한 리소스나 유용한 링크 AI chat 등

> 한 문장만 써도 충분

### 예시

- 사용자에게는 실제 속도보다 **느껴지는 반응**이 더 중요할 수 있다.
